import { TransaccionInterna } from './transaccion-interna';

describe('TransaccionInterna', () => {
  it('should create an instance', () => {
    expect(new TransaccionInterna()).toBeTruthy();
  });
});
